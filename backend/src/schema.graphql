type Query {
  card(id: String!): [Card]!
}

type Mutation {
  createCard(id: String!, url: String, text: String!, tag: String): Card!
}

type Card {
  id: String!
  url: String
  text: String!
  tag: String
}
